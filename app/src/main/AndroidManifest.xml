<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sahilmtl">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".navtr"
            android:label="@string/title_activity_navtr"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".otpresult" />
        <activity android:name=".otp" />
        <activity android:name=".mobile" />
        <activity android:name=".changepassword" />
        <activity android:name=".Photo" />
        <activity android:name=".newReg" />
        <activity android:name=".button1" />
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".home" />
        <activity android:name=".signup" />
        <activity android:name=".Welcome" />
    </application>

</manifest>